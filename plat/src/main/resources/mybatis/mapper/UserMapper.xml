<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rm.plat.mapper.UserMapper">
    <select id="queryUserByName" resultType="User" parameterType="String">
        select * from mybatis.user where username = #{name}
    </select>

    <select id="queryUserList" resultType="User">
        select * from user
    </select>

    <select id="queryUserById" resultType="User">
        select * from user where userid = #{userid}
    </select>

    <insert id="addUser" parameterType="User">
        insert into user(userid,username,password,perms,sex,userimgpath,birthday,email,signature,securityQ,securityA) values(#{userid},#{username},#{password},#{perms},#{sex},#{userimgpath},#{birthday},#{email},#{signature},#{securityQ},#{securityA})
    </insert>

    <update id="updateUser" parameterType="User">
        update user set username=#{username},password=#{password},perms=#{perms},sex=#{sex},userimgpath=#{userimgpath},birthday=#{birthday},email=#{email},signature=#{signature},securityQ=#{securityQ},securityA=#{securityA} where userid=#{userid}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from user where userid=#{userid}
    </delete>

    <select id="cnt" resultType="int">
        select count(*) from user
    </select>
</mapper>